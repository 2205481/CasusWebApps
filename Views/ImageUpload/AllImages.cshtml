@model List<ImageHandler>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Alle items</h2>
        </div>
    </div>
</div>

@if (Model.Any())
{
    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
<<<<<<< master
                <th>Url</th>
=======
                <th class="table-active">ItemType</th>
                <th class="table-active">Url</th>
                <th class="table-active"></th>
                <th class="table-active"></th>
>>>>>>> dev-robert
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.ImageUrl</td>
                    <td>
                        <a asp-controller="ImageUpload" asp-action="Details" asp-route-itemId="@item.Id">Details</a>
                    </td>
                    <td>
                        <a asp-controller="ImageUpload" asp-action="Delete" asp-route-itemid="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No items available.</p>
}

